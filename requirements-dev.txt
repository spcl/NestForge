# nest-forge dev/test deps for a LOCAL box: sibling checkouts ../dace, ../optarena, ../dace-fortran
# installed editable. Run `git -C ../dace switch extended` first. CI does not use this file -- it
# installs the same three editable from GitHub (see .github/workflows/ci.yml, Phase 2).

# Must be the `extended` branch: PyPI dace lacks the extended-only passes nest-forge calls
# (e.g. transformation.interstate.expand_nested_sdfg_inputs).
-e ../dace

# Not on PyPI at all -- only this checkout can resolve it.
-e ../optarena

# AFTER ../dace: its pyproject pins dace @ FaCe (a subset of extended); installing it first lets pip
# fetch that pin instead of reusing the extended checkout.
-e ../dace-fortran

# LAST, so the editable checkouts above are already installed and nothing resolves `dace` from PyPI.
-e .[dev]
